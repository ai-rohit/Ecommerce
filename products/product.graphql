type Query {
    products: [Product],
    product: Product
}

type Product{
    name: String!,
    descripiton: String!,
    price: Float!,
    quantity: Int!,
    quantityUnit: String!,
    category: [Category]
}