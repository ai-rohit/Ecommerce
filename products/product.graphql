type Query {
    products: [Product],
    product(id: String!): Product
}

type Product{
    id: String!
    name: String!,
    descripiton: String!,
    price: Float!,
    quantity: Int!,
    quantityUnit: String!,
    category: [Category]
}